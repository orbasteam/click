<% if notice %>
<div class="alert alert-success" role="alert">
  <button type="button" class="close" data-dismiss="alert" aria-label="Close">
    <span aria-hidden="true">&times;</span>
  </button>
  <%= notice %>
</div>
<% end %>

<h1>Tasks <%= link_to '<span class="glyphicon glyphicon-plus" aria-hidden="true"></span>'.html_safe, new_task_path, class: 'btn btn-primary' %></h1>
<table class="table table-striped">
	<tbody>
		<tr>
			<th>Channel</th>
			<th>Name</th>
			<th>Token</th>
			<th>Description</th>
			<th>Target Url</th>
			<th>Count</th>
			<th>Created at</th>
			<th>Updated at</th>
			<th>Actions</th>
		</tr>
		<% @tasks.each do |task| %>
		<tr>
			<td><%= task.channel.name %></td>
			<td><%= task.name %></td>
			<td><%= task.token %></td>
			<td><%= task.description %></td>
			<td><%= task.target_url %></td>
			<td><%= task.count %></td>
			<td><%= task.created_at %></td>
			<td><%= task.updated_at %></td>
			<td>
				<%= link_to '<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>'.html_safe, edit_task_path(task), class: 'btn btn-success' %>
				<%= link_to '<span class="glyphicon glyphicon-remove" aria-hidden="true"></span>'.html_safe, task, method: :delete, class: 'btn btn-danger' %>
			</td>
		</tr>
		<% end %>
	</tbody>
</table>